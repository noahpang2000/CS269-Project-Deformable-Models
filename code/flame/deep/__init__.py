"""PyTorch deep baselines: U-Net, DALS, and Deep Snake-style contour deformation.

Imported lazily so the classical pipeline (flame.kass / flame.gac) keeps working
without torch installed. Requires torch + torchvision; see requirements.txt.
"""
